name: CI (disabled automatic runs)

# This workflow used to run on push. It's now disabled for automatic runs to
# keep deployment as a single simple workflow. You can still run this manually
# from the Actions UI if you want to run the steps.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "CI automatic runs disabled. Use the 'Simple Deploy' workflow for push-to-deploy."
